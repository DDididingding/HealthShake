<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.jhgpt.model.dao.MemberDao">

    <!-- Member 목록 가져오기 -->
    <select id="selectAllMembers" resultType="Member">
        SELECT *
        FROM Member;
    </select>

    <!-- Member 정보를 조회한다. -->
    <select id="selectOneMember" parameterType="int" resultType="Member">
        SELECT *
        FROM Member
        WHERE member_code=#{member_code}
    </select>

    <!-- Member 등록 -->
    <insert id="insertMember" parameterType="Member">
        INSERT INTO Member(member_id, member_name, member_nickname, member_age, member_gender, member_password)
        VALUES(#{member_id}, #{member_name}, #{member_nickname}, #{member_age}, #{member_gender}, #{member_password})
    </insert>

    <!-- Member 삭제 -->
    <delete id="deleteMember" parameterType="int">
        DELETE FROM Member
        WHERE member_code=#{member_code}
    </delete>

    <!-- Member 수정 -->
    <update id="updateMember" parameterType="Member">
        UPDATE Member
        SET member_name=#{member_name}, member_nickname=#{member_nickname}, member_age=#{member_age},
            member_gender=#{member_gender}, member_status=#{member_status}
        WHERE member_code=#{member_code}
    </update>

</mapper>
